#!/bin/sh

dbpath='/eaas/server-data/db'
logpath='/home/bwfla/bw-fla-server/standalone/log/mongodb.log'

chown -R bwfla:bwfla "${dbpath}"
exec /sbin/setuser bwfla /usr/bin/mongod \
	--dbpath "${dbpath}" \
	--smallfiles \
	> "${logpath}"
